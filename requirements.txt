# データ解析の数学 ― デモノートブック
#   pip install -r requirements.txt
#
# Colab には最初から入っているので、Colab で使う場合は不要である。
# バージョンは「これ以上」の指定にしてある。新しすぎて動かない場合は
# 講義担当者に連絡すること。

numpy>=1.24
scipy>=1.10
matplotlib>=3.6
pandas>=1.5
scikit-learn>=1.2

# ノートブックを開くため（Colab では不要）
jupyterlab>=4.0

# 日本語フォント（無くても動く。図のラベルが英語になるだけ）
japanize-matplotlib>=1.1

# 第11章の最適輸送の節だけで使う。
# 入れなくてもよい（ノートブック内のセルが必要なときに導入する）。
pot>=0.9
